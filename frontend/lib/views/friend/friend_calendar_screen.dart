import 'package:flutter/material.dart';
import '../../models/user.dart';

class FriendCalendarScreen extends StatelessWidget {
  final User friend;

  const FriendCalendarScreen({super.key, required this.friend});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}